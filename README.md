I have created the README.md file for your project.

The README includes a comprehensive overview of the project, including its features, technologies used, instructions on how to run it, and the requested Limitations section.

Here is a summary of the limitations highlighted in the document:

Frontend Only (No Backend):

Form submissions (login and contact) are simulated on the client side and data isn't saved or emailed.

No Real Authentication:

The login page is a UI demonstration and doesn't actually log anyone in. Single URL Routing:

Because it's an SPA toggling views via JavaScript, the browser URL doesn't update (no hash routing), meaning you can't share direct links to specific sub-pages like the Login or Destinations view.

Hardcoded Content:

The destinations and images are static; there is no database or CMS feeding the site.
